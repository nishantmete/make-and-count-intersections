# make-and-count-intersections
Generates random lines on a black image, which in turn creates many intersections, which are then counted and returned by the program. 
See source_code.py and algorithm.txt 
